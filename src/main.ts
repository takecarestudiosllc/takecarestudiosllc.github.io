import './styles/main.css';
import { App } from './app/App';

// One entry point for every page; the page decides what it needs via
// <body data-page="..."> (see SceneManager for the scene registry).
new App();
